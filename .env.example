# 複製此檔為 .env 並填入你的真實金鑰。.env 已被 .gitignore 忽略，不會提交。
#
# 派網 API 金鑰：到派網網頁 -> 帳號 -> API 管理 建立。
# 建議：初期只勾選「讀取」與「現貨交易」權限，切勿開啟「提現」權限。
PIONEX_API_KEY=your_api_key_here
PIONEX_API_SECRET=your_api_secret_here

# (選用，最推薦) Discord 通知：在你的 Discord 頻道 → 編輯頻道 → 整合 → Webhook →
#   建立 Webhook → 複製 Webhook 網址，貼到下面即可。設定最簡單、免費、無限量。
#   注意：Webhook 是單向推播，收得到成交/錯誤/每日摘要，但不能傳訊息回來查狀態。
DISCORD_WEBHOOK_URL=

# (選用) Discord 雙向查詢：想在頻道打字就查網格狀態，才需要建「Bot」。免費。
#   1. https://discord.com/developers/applications → New Application → Bot → Reset Token 複製
#   2. Bot 頁面把「MESSAGE CONTENT INTENT」打開
#   3. OAuth2 → 勾 bot、權限給 View Channel + Read Message History + Send Messages，用產生的網址邀請進你的伺服器
#   4. Discord 設定開「開發者模式」後，右鍵頻道「複製頻道 ID」填 DISCORD_CHANNEL_ID
#   （DISCORD_USER_ID 選填：右鍵自己頭像複製使用者 ID，填了就只有你查得到狀態）
#   推播仍走上面的 Webhook；Bot 只負責「讀」你打的訊息。
DISCORD_BOT_TOKEN=
DISCORD_CHANNEL_ID=
DISCORD_USER_ID=

# (選用) Telegram 通知：到 @BotFather 建立 bot 取得 token，chat_id 可向 @userinfobot 查。
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# (選用) LINE 通知（用 Messaging API；舊的 LINE Notify 已停用）
# 到 https://developers.line.biz 建立 Messaging API channel 後取得：
#   LINE_CHANNEL_TOKEN：Messaging API 分頁的「Channel access token (long-lived)」
#   LINE_USER_ID      ：Basic settings 分頁的「Your user ID」（U 開頭）
# 並記得用手機把該官方帳號加為好友，才收得到推播。
LINE_CHANNEL_TOKEN=
LINE_USER_ID=

# (選用) Webhook 共享密鑰：訊號來源 POST 時需帶上，避免被陌生人觸發下單。
WEBHOOK_SECRET=
